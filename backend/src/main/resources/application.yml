spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cozyletters
    username: cozyletters
    password: cozyletters
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

jwt:
  secret: cozyletters-secret-key-that-is-at-least-256-bits-long-for-hs256
  expiration: 86400000
